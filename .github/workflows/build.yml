name: Runner Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, Window, X64, TwinCAT]

    steps:
      - name: Print information
        shell: pwsh
        run: |
          Write-Host "Runner is working!"
          Write-Host "Workspace: $env:GITHUB_WORKSPACE"
          hostname
          whoami
