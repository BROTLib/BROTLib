# Design docs

Living architecture/design docs, one per feature or subsystem. Kept around after
landing (`status: implemented`), not deleted.

- [twincat-scopeview-svdx-format.md](twincat-scopeview-svdx-format.md) — TwinCAT
  ScopeView `.svdx`/`.tcscopex` recording format and how to decode it
  (reverse-engineered from the MONETRoof roof-counter recording). *proposed*
  (Repos: BROTLib, MONETRoof)
